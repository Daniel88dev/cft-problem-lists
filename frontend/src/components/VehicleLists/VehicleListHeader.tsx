//import useVehicleListsContext from "./Store/VehicleListsContext.tsx";

const VehicleListHeader = () => {
  //const {loadInitialData, projects, loadVehicles} = useVehicleListsContext();
};

export default VehicleListHeader;
